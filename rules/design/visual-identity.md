# Identidade Visual — Regra de Preservação

1. **Nunca** alterar cores, fontes, logo ou tema sem pedido explícito
2. Carregar DS do projeto: `context/design-dna.md`, `AGENTS.md`, `frontend/ARCHITECTURE.md`, pasta `shared/ui/`
3. Greenfield sem DS → `design-system/design-dna-default.md`
4. Benchmark e referências modernas → **princípios apenas**, nunca cópia de marca/layout
5. Documentar no WC: `Identidade: preservada | evoluída | nova`
