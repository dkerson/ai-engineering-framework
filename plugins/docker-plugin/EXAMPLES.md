# Examples — Docker Plugin

## Exemplo 1 — Ativar no projeto

**Pedido:** Ative o plugin Docker para o projeto Umbra.

**Resultado:** `PROJECT_PLUGINS.md` atualizado; status draft mantido ate integracao real.

## Exemplo 2 — Resolver em missao

**Pedido:** [Pedido natural relacionado a Docker]

**Pipeline:** Plugin Resolver seleciona `docker-plugin` -> verifica permissoes -> encaminha ao Orchestrator.

> Integracao MCP real: backlog.
