# Usage — GitHub Plugin

1. `ENV.example` → `.env` local
2. `MCP_CONFIG_CURSOR.example.json` → `.cursor/mcp.json`
3. Token GitHub com escopo mínimo (repo read; write só se necessário)
4. Orchestrator: PR review, issues — read default

Push/merge: pedido explícito.
