# Firebase Plugin

> Status: **draft** — estrutura apenas; integracao real nao implementada.

Integracao Firestore, Auth e Storage via Firebase.

## Quick start

1. Verificar MCP em `MCP.md`
2. Configurar variaveis de ambiente (nunca commitar valores)
3. Ativar no projeto via Plugin Manager
4. Plugin Resolver seleciona automaticamente em missões relevantes

## Referencias

- `PLUGIN.md` — manifesto
- `plugins/PLUGIN_REGISTRY.md` — registro central
